# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
_autosummary

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.cache
.pytest_cache/
.mypy_cache/

# Logs / local config
*.log
local_settings.py
db.sqlite3
instance/
.webassets-cache

# Jupyter
.ipynb_checkpoints

# Environments
.env
.venv/
.venv2/
venv/
ENV/

# Editors
.idea

# datasets/ is tracked, except files over GitHub's 100 MB hard limit
# (restore raw_graphs_1000.pkl locally or use git-lfs).
datasets

# Generated run artifacts -- results CSV, route dumps, model weights,
# training logs -- are kept local only; regenerate by running the notebooks.
artifacts/
.tmp_pycache

# Local task-tracking / agent files (not part of the published repo)
CLAUDE.md
BOARD.md
board/
