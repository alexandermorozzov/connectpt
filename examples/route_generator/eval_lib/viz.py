"""Moved to the library: ``connectpt.routes_generator.reports.figures``.

Re-exported here so the eval_lib / notebook callers keep the ``eval_lib.viz``
names (single implementation now lives in the reports layer).
"""
from connectpt.routes_generator.reports.figures import *  # noqa: F401,F403
