# @package _global_
# E1 narrow (Mumford0): neural BCO vs Our NBCO alpha sweep (multi-method comparison
# via ExperimentBatch -- one run per method, each its own alpha sweep).
batch:
  name: e1_mumford0
  output_dir: artifacts/runs/e1/mumford0
  runs:
    - experiments/e1/mumford0/neural_bco
    - experiments/e1/mumford0/our_nbco
