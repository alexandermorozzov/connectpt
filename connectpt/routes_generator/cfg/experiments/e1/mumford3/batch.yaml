# @package _global_
# E1 narrow (Mumford3): neural BCO vs Our NBCO alpha sweep (multi-method comparison
# via ExperimentBatch -- one run per method, each its own alpha sweep).
batch:
  name: e1_mumford3
  output_dir: artifacts/runs/e1/mumford3
  runs:
    - experiments/e1/mumford3/neural_bco
    - experiments/e1/mumford3/our_nbco
