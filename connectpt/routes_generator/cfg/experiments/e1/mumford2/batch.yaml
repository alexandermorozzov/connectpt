# @package _global_
# E1 narrow (Mumford2): neural BCO vs Our NBCO alpha sweep (multi-method comparison
# via ExperimentBatch -- one run per method, each its own alpha sweep).
batch:
  name: e1_mumford2
  output_dir: artifacts/runs/e1/mumford2
  runs:
    - experiments/e1/mumford2/neural_bco
    - experiments/e1/mumford2/our_nbco

output:
  paper_stem: e1_mumford2
