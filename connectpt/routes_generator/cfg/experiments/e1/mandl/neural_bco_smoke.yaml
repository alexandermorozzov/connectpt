# @package _global_
# Smoke variant of e1/mandl/neural_bco: 2-iteration sweep for a fast dry-run (TEMP_ outputs).
# Config-first (inherit full config + override the budget), no code substitution.
defaults:
  - neural_bco
  - _self_

run:
  name: e1/mandl/neural_bco_smoke

paths:
  output_dir: artifacts/runs/e1/mandl/neural_bco_smoke

sweep:
  n_iterations: 2
