# @package _global_
# E2 Fig-2 5-model ablation (Mumford1): trim 12 + extend 12, adjustment OFF
# (adj_weight=0). Replaces the flat e2_5model method configs.
defaults:
  - /search: bee_colony_base
  - /search/models: construction_and_edit_seeded
  - /search/bee_sets: trim12_extend12
  - _self_

run:
  name: e2/5model/mumford1/trim12_extend12
  label: trim 12 + extend 12
  seed: 0
  cpu: false

paths:
  output_dir: artifacts/runs/e2/5model/mumford1/trim12_extend12

# n_bees must match the bee-set total.
search:
  n_bees: 24

data:
  source: benchmark
  city: Mumford1

sweep:
  alpha: [0.0, 0.25, 0.5, 0.75, 1.0]
  adj_target: 0.2
  adj_weight: 0.0    # 0 = adjustment penalty OFF
  n_iterations: 200

metrics: [RTT, WMC, ATT, adj_vs_seed, "redun%", cost]
