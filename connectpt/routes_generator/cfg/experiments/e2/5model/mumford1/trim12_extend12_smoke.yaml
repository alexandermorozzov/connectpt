# @package _global_
# Smoke variant of e2/5model/mumford1/trim12_extend12: 2-iteration sweep for a fast dry-run (TEMP_ outputs).
# Config-first (inherit full config + override the budget), no code substitution.
defaults:
  - trim12_extend12
  - _self_

run:
  name: e2/5model/mumford1/trim12_extend12_smoke

paths:
  output_dir: artifacts/runs/e2/5model/mumford1/trim12_extend12_smoke

sweep:
  n_iterations: 2
