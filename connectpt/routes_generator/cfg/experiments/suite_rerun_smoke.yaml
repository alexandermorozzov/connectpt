# @package _global_
# Smoke variant of suite_rerun: the 1-iteration budget (from suite_smoke, single
# city, TEMP_ prefix) but paper deliverables still land in artifacts/reruns/.
# Purpose: verify the rerun plumbing writes logs/routes/histories/tables end to
# end without a multi-hour run.
defaults:
  - /experiments/suite_smoke
  - _self_

paper_output_dir: artifacts/reruns
