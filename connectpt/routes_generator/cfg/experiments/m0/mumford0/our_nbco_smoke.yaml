# @package _global_
# Smoke variant of m0/mumford0/our_nbco: 2-iteration sweep (TEMP_ dry-run). Config-first.
defaults:
  - our_nbco
  - _self_
run:
  name: m0/mumford0/our_nbco_smoke
paths:
  output_dir: artifacts/runs/m0/mumford0/our_nbco_smoke
sweep:
  n_iterations: 2
