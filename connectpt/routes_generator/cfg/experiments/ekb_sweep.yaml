# Unified experiment spec: EKB alpha sweep (was cells 36/38's Python loop).
name: ekb_sweep
data:
  source: ekb
method:
  config: ekb/our_nbco_ekb
  force_cpu: true
  process_neural_bees_sequentially: true
method_label: Our NBCO (GNN + trim/extend)
sweep:
  alpha: [0.0, 0.5, 1.0]
  adj_target: 0.3
  n_iterations: 50
metrics: [RTT, WMC, ATT, adj_vs_seed, "redun%", cost]
report:
  routes_plot: gis
