# Unified experiment spec: MACSA Table B alpha sweep.
name: macsa_sweep
data:
  source: macsa
  scenario: Mandl8
method:
  config: nbco_variants/our_nbco_mandl
method_label: Our NBCO (GNN + trim/extend)
sweep:
  alpha: [0.0, 0.5, 1.0]
  adj_target: 0.3
  n_iterations: 100
metrics: [RTT, WMC, ATT, adj_vs_seed, "redun%", cost]
report:
  routes_plot: network
