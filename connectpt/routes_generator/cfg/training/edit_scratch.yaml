# @package _global_
# The paper's from-scratch edit-model run (was the notebook's RUN_NAME /
# TRAIN_FROM_SCRATCH / N_ITERATIONS magic constants). Overlays train/edit.yaml.
defaults:
  - /training/edit
  - _self_

run:
  name: NEW_lc_copytiers_curric_noadj_v1

paths:
  weights_dir: artifacts/model_weights
  checkpoint_path: improvement/NEW_lc_copytiers_curric_noadj_v1.pt
  init_checkpoint_path: null   # from scratch (random init)
